.DS_Store
*.log

# Insight files can contain private repository plans, names, and schedules. Keep them
# local unless a maintainer deliberately force-adds a reviewed example.
insights/*.json
!insights/brettknowlton__vibe-git.json
